# Система авторизации и профиля Forest Rest

## 📁 Структура файлов

```
/src/
├── layouts/
│   ├── auth.vue                        # Лейаут для страниц авторизации
│   └── styles/layoutAuth/
│       └── layout-auth.scss           # Стили лейаута
│
├── pages/
│   ├── auth.vue                       # Страница авторизации/регистрации
│   ├── profile/
│   │   ├── index.vue                  # Главная страница профиля
│   │   └── admin-stats.vue            # Админ-панель со статистикой
│   └── styles/
│       ├── pageAuth/
│       │   └── page-auth.scss         # Стили страницы авторизации
│       ├── pageProfile/
│       │   └── page-profile.scss      # Стили профиля
│       └── pageAdminStats/
│           └── page-admin-stats.scss  # Стили админ-панели
│
└── components/ui/
    └── uiCheckbox/
        └── UiCheckbox.vue             # Обновлен (добавлен слот label)
```

## 🎨 Страницы

### 1. Авторизация `/auth`
**Лейаут:** `auth`

**Функционал:**
- ✅ Форма входа (email, пароль)
- ✅ Форма регистрации (имя, email, телефон, пароль, подтверждение пароля)
- ✅ Переключение между формами через табы
- ✅ Валидация полей с отображением ошибок
- ✅ Чекбокс "Запомнить меня" (вход)
- ✅ Ссылка "Забыли пароль?"
- ✅ Согласие с условиями использования (регистрация)
- ✅ Редирект на `/profile` после успешной авторизации

**Используемые компоненты:**
- `UiInput` - поля ввода
- `UiInputPhone` - поле для телефона с маской
- `UiButton` - кнопки
- `UiCheckbox` - чекбоксы

### 2. Профиль `/profile`
**Лейаут:** `default`

**Функционал:**
- ✅ Личная информация (имя, email, телефон)
- ✅ История заказов (заглушка)
- ✅ Сохраненные адреса (заглушка)
- ✅ Кнопка редактирования профиля
- ✅ Кнопка выхода из аккаунта
- ✅ Карточка админ-панели (если пользователь - администратор)

**Блок администратора:**
- Отображается только для пользователей с правами администратора
- Содержит кнопку перехода к статистике заказов
- Выделен цветом и иконкой

### 3. Админ-панель `/profile/admin-stats`
**Лейаут:** `default`

**Функционал:**
- ✅ 4 карточки с общей статистикой
- ✅ Переключатель периодов (сегодня, неделя, месяц, год, все время)
- ✅ Поиск по заказам
- ✅ Фильтры (статус, способ оплаты)
- ✅ Таблица со всеми заказами
- ✅ Цветовое кодирование статусов и способов оплаты
- ✅ Действия над заказами (просмотр, редактирование)
- ✅ Пагинация
- ✅ Кнопка возврата в профиль

## 🔐 Валидация форм

### Форма входа
```typescript
// Email
- Обязательное поле
- Формат email (regex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/)

// Пароль
- Обязательное поле
- Минимум 6 символов
```

### Форма регистрации
```typescript
// Имя
- Обязательное поле
- Минимум 2 символа

// Email
- Обязательное поле
- Формат email

// Телефон
- Обязательное поле
- Маска: +7 (999) 999-99-99

// Пароль
- Обязательное поле
- Минимум 6 символов

// Подтверждение пароля
- Обязательное поле
- Должен совпадать с паролем

// Согласие с условиями
- Обязательный чекбокс
```

## 🎯 Роутинг

```typescript
/auth                     # Страница авторизации
├── layout: auth
└── redirect: /profile (после успешной авторизации)

/profile                  # Главная страница профиля
├── layout: default
└── access: авторизованные пользователи

/profile/admin-stats      # Админ-панель
├── layout: default
└── access: администраторы
```

## 🔒 Контроль доступа

### Текущая реализация (демо)
```typescript
// В /profile/index.vue
const isAdmin = ref(true); // Для демонстрации
```

### Для продакшн-версии

#### 1. Создать стор пользователя
```typescript
// stores/user.ts
export const useUserStore = defineStore('user', {
  state: () => ({
    user: null,
    isAuthenticated: false,
    isAdmin: false,
  }),
  
  actions: {
    async login(credentials) {
      // API запрос
      const response = await $fetch('/api/auth/login', {
        method: 'POST',
        body: credentials,
      });
      
      this.user = response.user;
      this.isAuthenticated = true;
      this.isAdmin = response.user.role === 'admin';
    },
    
    logout() {
      this.user = null;
      this.isAuthenticated = false;
      this.isAdmin = false;
    },
  },
});
```

#### 2. Создать middleware
```typescript
// middleware/auth.ts
export default defineNuxtRouteMiddleware((to, from) => {
  const user = useUserStore();
  
  if (!user.isAuthenticated) {
    return navigateTo('/auth');
  }
});

// middleware/admin.ts
export default defineNuxtRouteMiddleware((to, from) => {
  const user = useUserStore();
  
  if (!user.isAuthenticated) {
    return navigateTo('/auth');
  }
  
  if (!user.isAdmin) {
    return navigateTo('/profile');
  }
});
```

#### 3. Применить middleware к страницам
```vue
<!-- pages/profile/index.vue -->
<script setup lang="ts">
definePageMeta({
  middleware: 'auth'
});
</script>

<!-- pages/profile/admin-stats.vue -->
<script setup lang="ts">
definePageMeta({
  middleware: ['auth', 'admin']
});
</script>
```

## 🌐 API интеграция

### Необходимые эндпоинты

#### Авторизация
```typescript
// Вход
POST /api/auth/login
Body: { email, password, remember }
Response: { user, token }

// Регистрация
POST /api/auth/register
Body: { name, email, phone, password }
Response: { user, token }

// Выход
POST /api/auth/logout
Response: { success: true }

// Восстановление пароля
POST /api/auth/forgot-password
Body: { email }
Response: { success: true }
```

#### Профиль
```typescript
// Получение профиля
GET /api/user/profile
Response: { user }

// Обновление профиля
PATCH /api/user/profile
Body: { name, email, phone }
Response: { user }

// Получение заказов пользователя
GET /api/user/orders?page=1&limit=10
Response: { orders, total, pages }

// Получение адресов
GET /api/user/addresses
Response: { addresses }
```

#### Админ-панель
```typescript
// Статистика
GET /api/admin/statistics?period=month
Response: {
  totalOrders,
  totalRevenue,
  averageCheck,
  totalCustomers,
  changePercents: { orders, revenue, check, customers }
}

// Заказы
GET /api/admin/orders?page=1&limit=10&status=&payment=&search=
Response: { orders, total, pages }

// Детали заказа
GET /api/admin/orders/:id
Response: { order }

// Обновление заказа
PATCH /api/admin/orders/:id
Body: { status, ... }
Response: { order }
```

## 🎨 Стилистика

### Цвета
- **Основной:** `--green` (#2A5F2B)
- **Акцентный:** `--green-soft` (#3E823F)
- **Успех:** `--green`
- **Ошибка:** `--red` (#FF4556)
- **Предупреждение:** Оранжевый (#FF8C00)
- **Информация:** `--blue` (#5897FB)

### Шрифты
- **Заголовки:** Rubik (font-title)
- **Текст:** Nunito (font-text)

### Адаптивность
- **Мобильные:** 320px - 767px
- **Планшеты:** 768px - 1099px
- **Десктоп:** 1100px+

## ✨ Особенности реализации

### 1. Лейауты
- **auth.vue** - минималистичный лейаут без каталога и корзины
- **default.vue** - основной лейаут с навигацией

### 2. Анимации
- Плавные переходы между формами (tabs)
- Hover-эффекты на кнопках и карточках
- Анимация открытия dropdown-списков

### 3. Валидация
- Реалтайм валидация при вводе
- Отображение ошибок под полями
- Блокировка кнопки отправки при ошибках

### 4. UX улучшения
- Автофокус на первое поле формы
- Отображение/скрытие пароля (можно добавить)
- Автозаполнение данных браузером
- Доступность (ARIA-метки)

## 📱 Мобильная версия

### Оптимизация для мобильных
- Адаптивная сетка карточек
- Горизонтальная прокрутка таблицы на мобильных
- Увеличенные кликабельные области
- Оптимизированный размер шрифтов

## 🔄 Статус реализации

### ✅ Готово
- [x] Лейаут авторизации
- [x] Страница авторизации/регистрации
- [x] Валидация форм
- [x] Страница профиля
- [x] Админ-панель со статистикой
- [x] Фильтрация и поиск заказов
- [x] Адаптивный дизайн
- [x] Обновление UiCheckbox (слот label)

### 🚧 Требуется доработка
- [ ] API интеграция
- [ ] Middleware для защиты маршрутов
- [ ] Стор для управления состоянием пользователя
- [ ] Восстановление пароля
- [ ] OAuth (Google, VK)
- [ ] Редактирование профиля
- [ ] Управление адресами
- [ ] Детальный просмотр заказов

### 🎯 Будущие улучшения
- [ ] Двухфакторная аутентификация
- [ ] Аватар пользователя
- [ ] История активности
- [ ] Настройки уведомлений
- [ ] Темная тема
- [ ] Графики в админ-панели
- [ ] Экспорт данных

## 📝 Примеры использования

### Переход на страницу авторизации
```vue
<NuxtLink to="/auth">
  Войти
</NuxtLink>

<!-- или -->

<UiButton
  tag="link"
  href="/auth"
  view="primary"
>
  Войти
</UiButton>
```

### Проверка авторизации
```typescript
const user = useUserStore();

if (user.isAuthenticated) {
  // Пользователь авторизован
}

if (user.isAdmin) {
  // Пользователь - администратор
}
```

### Выход из аккаунта
```typescript
const handleLogout = () => {
  const user = useUserStore();
  user.logout();
  navigateTo('/auth');
};
```

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024  
**Автор:** Forest Rest Development Team

