# 🗺️ Карта маршрутов Forest Rest

## 📍 Основные страницы

### Публичные страницы
```
/                           # Главная страница
├── layout: default
└── Каталог товаров, категории

/menu/:id                   # Страница категории меню
├── layout: default
└── Список товаров категории

/about                      # О ресторане
├── layout: default
└── Информация о ресторане

/contacts                   # Контакты
├── layout: default
└── Контактная информация

/delivery                   # Доставка
├── layout: default
└── Условия доставки

/legal-information          # Правовая информация
├── layout: default
└── Пользовательское соглашение, политика конфиденциальности
```

## 🔐 Авторизация

```
/auth                       # Авторизация и регистрация
├── layout: auth           # Специальный лейаут без навигации
├── Форма входа
├── Форма регистрации
└── redirect: /profile     # После успешной авторизации
```

## 👤 Личный кабинет

```
/profile                    # Профиль пользователя
├── layout: default
├── access: authenticated  # Только для авторизованных
├── Личная информация
├── История заказов
├── Сохраненные адреса
└── [admin] Ссылка на панель администратора
```

## 🛒 Заказы

```
/cart                       # Корзина
├── layout: default
├── Список товаров в корзине
├── Способ оплаты
├── Форма оформления заказа
└── Кнопка "Оформить заказ"
```

## 👨‍💼 Администратор

```
/profile/admin-stats        # Панель администратора
├── layout: default
├── access: admin          # Только для администраторов
├── Статистика заказов (4 карточки)
├── Фильтры по периодам
├── Поиск и фильтрация заказов
├── Таблица всех заказов
└── Управление заказами
```

## 🎨 Лейауты

### `default.vue`
Основной лейаут для всех страниц:
- BaseHeader (шапка с логотипом, переключатель доставки)
- BaseCatalog (навигация по категориям)
- Контент страницы
- BaseWidgetCart (виджет корзины, если есть товары)
- BaseFooter (подвал)

### `auth.vue`
Минималистичный лейаут для авторизации:
- Логотип с ссылкой на главную
- Контент страницы (формы входа/регистрации)
- Простой футер с копирайтом
- Градиентный фон

## 🔄 Переходы между страницами

### Сценарий 1: Новый пользователь
```
/ (главная)
  ↓ [клик на товар]
/menu/:id
  ↓ [добавить в корзину]
/cart
  ↓ [оформить заказ → требуется авторизация]
/auth
  ↓ [регистрация]
/profile
  ↓ [вернуться к оформлению]
/cart
  ↓ [оформить заказ]
✓ Заказ оформлен
```

### Сценарий 2: Администратор
```
/auth
  ↓ [вход с правами администратора]
/profile
  ↓ [видна карточка "Панель администратора"]
  ↓ [клик на "Перейти к статистике"]
/profile/admin-stats
  ↓ [просмотр статистики и управление заказами]
```

### Сценарий 3: Возвращающийся пользователь
```
/ (главная)
  ↓ [клик на иконку профиля в шапке]
/profile
  ↓ [просмотр личной информации]
  ↓ [просмотр истории заказов]
```

## 🔗 Важные ссылки в компонентах

### BaseHeader
```vue
<!-- Логотип -->
<NuxtLink to="/">
  <img src="logo.png" />
</NuxtLink>

<!-- Профиль -->
<UiButtonIcon
  tag="link"
  href="/profile"
  iconName="bx bx-user"
/>
```

### BaseFooter
```vue
<NuxtLink to="/about">О нас</NuxtLink>
<NuxtLink to="/delivery">Доставка</NuxtLink>
<NuxtLink to="/contacts">Контакты</NuxtLink>
<NuxtLink to="/legal-information">Правовая информация</NuxtLink>
```

### BaseCatalog
```vue
<NuxtLink to="/menu/1">Римская пицца</NuxtLink>
<NuxtLink to="/menu/2">Роллы</NuxtLink>
<NuxtLink to="/menu/3">Гриль</NuxtLink>
<!-- ... другие категории -->
```

## 📱 Навигация с помощью роутера

### В компонентах
```typescript
const router = useRouter();

// Программная навигация
router.push('/profile');
router.push('/auth');
router.push({ path: '/menu/1' });

// С параметрами
router.push({
  path: '/profile/admin-stats',
  query: { period: 'month' }
});
```

### С помощью NuxtLink
```vue
<!-- Простая ссылка -->
<NuxtLink to="/profile">Профиль</NuxtLink>

<!-- С классами -->
<NuxtLink 
  to="/auth" 
  class="link"
  activeClass="link--active"
>
  Войти
</NuxtLink>

<!-- Внешняя ссылка -->
<NuxtLink 
  to="https://example.com" 
  external
  target="_blank"
>
  Внешняя ссылка
</NuxtLink>
```

### С помощью UiButton
```vue
<!-- Кнопка-ссылка -->
<UiButton
  tag="link"
  href="/profile"
  view="primary"
>
  Перейти в профиль
</UiButton>

<!-- Обычная кнопка с событием -->
<UiButton
  @click="router.push('/auth')"
  view="default"
>
  Войти
</UiButton>
```

## 🛡️ Защита маршрутов

### Текущая реализация
Защита отсутствует (демо-версия)

### Рекомендуемая реализация
```typescript
// middleware/auth.ts
export default defineNuxtRouteMiddleware((to, from) => {
  const user = useUserStore();
  
  const protectedRoutes = ['/profile', '/cart'];
  const isProtected = protectedRoutes.some(route => 
    to.path.startsWith(route)
  );
  
  if (isProtected && !user.isAuthenticated) {
    return navigateTo('/auth');
  }
});

// middleware/admin.ts
export default defineNuxtRouteMiddleware((to, from) => {
  const user = useUserStore();
  
  if (to.path.startsWith('/profile/admin') && !user.isAdmin) {
    return navigateTo('/profile');
  }
});
```

### Применение middleware
```vue
<!-- nuxt.config.ts -->
export default defineNuxtConfig({
  router: {
    middleware: 'auth' // Глобально
  }
})

<!-- или в компоненте страницы -->
<script setup lang="ts">
definePageMeta({
  middleware: ['auth', 'admin']
});
</script>
```

## 🎯 SEO и мета-теги

Все страницы используют `useSeo()` для установки мета-тегов:

```typescript
useSeo({
  title: 'Название страницы',
  description: 'Описание страницы',
  keywords: ['ключевое', 'слово'],
  type: 'website',
  noIndex: false // true для закрытых страниц
});
```

### Закрыты от индексации
- `/auth` (noIndex: true)
- `/profile` (noIndex: true)
- `/profile/admin-stats` (noIndex: true)
- `/cart` (noIndex: true)

### Открыты для индексации
- `/` (главная)
- `/menu/:id` (категории)
- `/about`
- `/delivery`
- `/contacts`
- `/legal-information`

## 📊 Аналитика переходов

### Рекомендуемые события для отслеживания
```typescript
// Авторизация
- auth_login_success
- auth_register_success
- auth_logout

// Профиль
- profile_view
- profile_edit
- address_add

// Админ
- admin_stats_view
- admin_order_view
- admin_order_edit

// Заказы
- cart_view
- order_complete
```

---

**Всего маршрутов:** 8 страниц + 2 админ-страницы  
**Лейаутов:** 2 (default, auth)  
**Последнее обновление:** 2024

